<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MCup.Views.ListaStrutture">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="listViewStrutture" ItemSelected="listViewStrutture_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout VerticalOptions="Center" Orientation="Horizontal">
                                <Image Source="{Binding ImageStruttura}"></Image>
                                <Label Text="{Binding MenuTitle}" TextColor="Black" HorizontalTextAlignment="Center">
                                </Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>