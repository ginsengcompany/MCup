<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xfx="clr-namespace:Xfx;assembly=Xfx.Controls"
             x:Class="MCup.Views.Registrazione"
            Title="Registrazione"
              x:Name="caro" >
    
    <ContentPage Padding="10" x:Name="page0">
        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <StackLayout VerticalOptions="StartAndExpand">
                <Label HorizontalOptions="Center" FontSize="30" FontAttributes="Bold" Text="Dati utente" TextColor="#0971B2"></Label>

            </StackLayout>
            <StackLayout Orientation="Vertical" VerticalOptions="CenterAndExpand" Padding="15" >
                <Grid HorizontalOptions="FillAndExpand">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.2*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="0.1*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                        <Image Source="user.png"  VerticalOptions="Center" HorizontalOptions="Start" HeightRequest="50" WidthRequest="50" Grid.Column="0" Grid.Row="0"></Image>
                   
                    <StackLayout Grid.Column="1" Grid.Row="0" VerticalOptions="Center">

                        <Label 
                                Text="Username" 
                                FontAttributes="Bold" 
                                TextColor="Black" 
                                FontSize="Large"/>
                        <Entry 
                            Placeholder="Inserire Username*" 
                            Text="{Binding Username}" 
                            WidthRequest="250"/>
                        <Label 
                            Text="Attenzione, campo obbligatorio" 
                            IsVisible="{Binding NameErrorUsername}" 
                            TextColor="Red" 
                            FontSize="Small"/>
                    </StackLayout>
                    <Image Source="info.png" HeightRequest="30" VerticalOptions="Center" WidthRequest="30" Grid.Column="2" Grid.Row="0">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer Tapped="infoUsername"></TapGestureRecognizer>
                        </Image.GestureRecognizers>
                    </Image>
                </Grid>
               
               <StackLayout Margin="0,10,0,0">
                   <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>

                        </Grid.RowDefinitions>
                   <Image Source="passwor.png" Grid.Column="0" Grid.Row="0" VerticalOptions="Center" HeightRequest="50" WidthRequest="50"></Image>
                        <StackLayout  Grid.Column="1"
                                      Grid.Row="0" VerticalOptions="Center">
                           <Label Text="Password"
                                  FontAttributes="Bold"
                                  TextColor="Black" 
                                  FontSize="Large"/>
                           <xfx:XfxEntry 
                               Placeholder="Inserire password*" 
                               Text="{Binding password}" 
                               ErrorText="{Binding NameErrorTextPassword}" 
                               ErrorDisplay="Underline" 
                               IsPassword="True" 
                               WidthRequest="250"/>
                        </StackLayout>
                
                   <xfx:XfxEntry 
                       Grid.Column="1" Grid.Row="1"
                       Placeholder="Reinserire password*" 
                       Text="{Binding ConfermaPassword}" 
                       ErrorText="{Binding NameErrorTextConfermaPassword}" 
                       ErrorDisplay="Underline" 
                       IsPassword="True" 
                       WidthRequest="250"/>
                    </Grid>
                </StackLayout>
                <StackLayout VerticalOptions="EndAndExpand" HorizontalOptions="Center">
                    <Label Text="* campi obbligatori" FontSize="Small"/> 
                </StackLayout>
                
            </StackLayout>
            <StackLayout Orientation="Horizontal" Spacing="5" VerticalOptions="EndAndExpand" HorizontalOptions="End" >
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Tapped="AvantiPrimaPagina"></TapGestureRecognizer>
                </StackLayout.GestureRecognizers>
                <Label Text="AVANTI"/>
                <Image Source="frecciadx.png"/>
            </StackLayout>
        </StackLayout>
       
    </ContentPage>




    <ContentPage Padding="20" x:Name="page1" InputTransparent="False">
        
        <StackLayout>
            
            <StackLayout VerticalOptions="Start">
                <Label HorizontalOptions="Center" FontSize="30" FontAttributes="Bold" Text="Dati anagrafici" TextColor="#0971B2"></Label>

            </StackLayout>
            <ScrollView>
                <StackLayout VerticalOptions="CenterAndExpand">

                    <Label Text="Nome" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                    <Entry Placeholder="Inserire nome*" Text="{Binding nome}" WidthRequest="250"/>
                    <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorNome}" TextColor="Red" FontSize="Small"/>

                    <Label Text="Cognome" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                    <Entry Placeholder="Inserire cognome*" Text="{Binding cognome}" WidthRequest="250"/>
                    <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorCognome}" TextColor="Red" FontSize="Small"/>

                    <StackLayout Margin="5">
                        <Label Text="Codice Fiscale" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Entry x:Name="entryCodiceFiscale" HorizontalOptions="StartAndExpand" Placeholder="Inserire codice fiscale*" Text="{Binding codiceFiscale}"  WidthRequest="600" Grid.Row="0"/>
                            <Image Source="iconaScan.png" WidthRequest="40" HorizontalOptions="End" Grid.Row="0" Grid.Column="1">
                                <Image.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="Button_Clicked" NumberOfTapsRequired="1" />
                                </Image.GestureRecognizers>
                            </Image>
                        </Grid>
                        <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorCodFiscale}" TextColor="Red" FontSize="Small"/>

                    </StackLayout>


                    <Label Text="Data di nascita*" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                    <DatePicker x:Name="pickerNascita" Date="{Binding data_nascita}" Format="dd/MM/yyyy" />
                    <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorDataNascita}" TextColor="Red" FontSize="Small"/>

                    <Label Text="Provincia di nascita*" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                    <Picker x:Name="pickerProvinciaNascita" Title="Seleziona la provincia di nascita" SelectedIndexChanged="Picker_OnSelectedIndexChangedProvinciaNascita" ItemsSource="{Binding listaProvince}" ItemDisplayBinding="{Binding provincia}"/>
                    <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorProvinciaNascita}" TextColor="Red" FontSize="Small"/>


                    <Label Text="Comune di nascita*" FontAttributes="Bold" TextColor="Black" FontSize="Medium" HorizontalOptions="StartAndExpand"/>
                    <Picker x:Name="PickerComuneNascita" Title="Seleziona il comune di nascita" SelectedIndexChanged="Picker_OnSelectedIndexChanged" ItemsSource="{Binding listaComuni}" ItemDisplayBinding="{Binding nome}"/>
                    <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorComuneNascita}" TextColor="Red" FontSize="Small"/>
                </StackLayout>
            </ScrollView>
            <StackLayout VerticalOptions="EndAndExpand" HorizontalOptions="Center">
                <Label Text="* campi obbligatori" FontSize="Small"/>
            </StackLayout>
            <StackLayout Margin="5" VerticalOptions="EndAndExpand">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <StackLayout Orientation="Horizontal" Spacing="5" VerticalOptions="End" HorizontalOptions="End" Grid.Column="1" Grid.Row="0" >
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="AvantiSecondaPagina"></TapGestureRecognizer>
                    </StackLayout.GestureRecognizers>
                    <Label Text="AVANTI"/>
                    <Image Source="frecciadx.png"/>
                </StackLayout>
                <StackLayout Orientation="Horizontal" Spacing="5" VerticalOptions="End" HorizontalOptions="Start"  Grid.Column="0" Grid.Row="0" >
                    <StackLayout.GestureRecognizers>
                        <TapGestureRecognizer Tapped="IndietroPrimaPagina"></TapGestureRecognizer>
                    </StackLayout.GestureRecognizers>
                    <Image Source="frecciadx.png" Rotation="180"/>
                    <Label Text="INDIETRO"/>
                </StackLayout>
            </Grid>
        </StackLayout>

        </StackLayout>
    </ContentPage>

    <ContentPage Padding="20"  x:Name="page3">
        <StackLayout>
            <StackLayout VerticalOptions="StartAndExpand">
                <Label HorizontalOptions="Center" FontSize="30" FontAttributes="Bold" Text="Dati Utente" TextColor="#0971B2"></Label>
            </StackLayout>
            <ScrollView>
            <StackLayout VerticalOptions="CenterAndExpand">
                <Label Text="Provincia di residenza*" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                    <Picker x:Name="pickerProvinciaResidenza" Title="Seleziona la provincia di residenza" SelectedIndexChanged="Picker_OnSelectedIndexChangedProvinciaResidenza" ItemsSource="{Binding listaProvince}" ItemDisplayBinding="{Binding provincia}" />
                <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorProvinciaResidenza}" TextColor="Red" FontSize="Small"/>


                <Label Text="comune di residenza*" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                <Picker x:Name="PickerComuneResidenza" Title="Seleziona il comune di residenza" SelectedIndexChanged="Picker_OnSelectedIndexChanged1" ItemsSource="{Binding listaComuniResidenza}" ItemDisplayBinding="{Binding nome}"/>
                <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorComuneResidenza}" TextColor="Red" FontSize="Small"/>

                <Label Text="Indirizzo*" FontAttributes="Bold" TextColor="Black" FontSize="Medium" HorizontalOptions="StartAndExpand"/>
                <Entry Placeholder="Inserire indirizzo" Text="{Binding Indirizzo}" WidthRequest="250"/>
                <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorIndirizzo}" TextColor="Red" FontSize="Small"/>


                <Label Text="Numero Telefonia mobile*" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                <Entry Placeholder="Inserire numero di telefono mobile" Text="{Binding telefono}" Keyboard="Numeric" WidthRequest="250"/>
                <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorTelefono}" TextColor="Red" FontSize="Small"/>

                <Label Text="Inserisci la tua E-mail" FontAttributes="Bold" TextColor="Black" FontSize="Medium" HorizontalOptions="StartAndExpand"/>
                <Entry Placeholder="Inserire email" Text="{Binding Email}" WidthRequest="250"/>

                <Label Text="StatoCivile" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                <Picker x:Name="PickerUnione" Title="Seleziona lo stato civile" ItemsSource="{Binding ListaStatoCivile}" SelectedIndexChanged="Picker_OnSelectedIndexChangedSceltaUnione" ItemDisplayBinding="{Binding descrizione}"/>



                <Label Text="Sesso*" FontAttributes="Bold" TextColor="Black" FontSize="Medium"/>
                <Picker x:Name="sexPicker" Title="Seleziona sesso" SelectedItem="{Binding sceltaSesso}">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:Char}">
                            <x:Char>F</x:Char>
                            <x:Char>M</x:Char>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
                <Label Text="Attenzione, campo obbligatorio" IsVisible="{Binding NameErrorSesso}" TextColor="Red" FontSize="Small"/>


               

            </StackLayout>
            </ScrollView>

            <StackLayout VerticalOptions="EndAndExpand" HorizontalOptions="Center">
                <Label Text="* campi obbligatori" FontSize="Small"/>
            </StackLayout>
            <StackLayout Margin="5" VerticalOptions="EndAndExpand">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <StackLayout Spacing="5" Grid.Column="1" Grid.Row="0"  HorizontalOptions="End" >
                        <Button Text="Registrati" Command="{Binding registrati}" BackgroundColor="#0971B2" BorderColor="#0971B2" TextColor="White" Margin="15"/>
                    </StackLayout>
                    <StackLayout Orientation="Horizontal" Spacing="5"  HorizontalOptions="Start" Grid.Column="0" Grid.Row="0">
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Tapped="IndietroSecondaPagina"></TapGestureRecognizer>
                        </StackLayout.GestureRecognizers>
                        <Image Source="frecciadx.png"  Rotation="180" VerticalOptions="Center"/>
                        <Label Text="INDIETRO" VerticalOptions="Center"/>
                    </StackLayout>
                </Grid>
            </StackLayout>
        </StackLayout>
        
    </ContentPage>
</CarouselPage>